import { LoginDisableDirective } from './login-disable.directive';

describe('LoginDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginDisableDirective();
    expect(directive).toBeTruthy();
  });
});
